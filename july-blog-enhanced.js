(() => {
  'use strict';

  let timer = null;
  let analysis = null;

  const escapeHTML = (value = '') => String(value).replace(/[&<>"']/g, (ch) => ({
    '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;'
  })[ch]);

  const enhancedDraft = `안녕하세요! 블루블랙 펜샵 Kevin입니다.

오늘은 오퍼스88 만년필을 기다려주셨던 분들께 반가운 재입고 소식을 전해드리려고 합니다!

개성 있는 컬러와 큼직한 바디, 잉크가 담긴 모습을 그대로 감상할 수 있는 디자인으로 꾸준히 사랑받아온 오퍼스88 만년필이 블블샵에 다시 찾아왔습니다.✨

[사진 1 | 재입고된 오퍼스88 만년필 전체 대표 컷]
[사진 2 | 오퍼스88 로고와 캡 디테일]
[사진 3 | 재입고 제품을 한 줄로 배열한 전체 컷]

오퍼스88는 모델마다 바디의 형태와 컬러 조합이 뚜렷해 같은 브랜드 안에서도 취향에 맞는 한 자루를 고르는 재미가 큰데요.

화려한 패턴을 담은 모델부터 차분하고 클래식한 컬러, 잉크가 보이는 투명한 데모 바디까지 서로 다른 매력을 한자리에서 비교해 볼 수 있습니다.

이번에 다시 만날 수 있는 제품들을 하나씩 함께 살펴볼까요?👀

오퍼스88 하모니 만년필

부드러운 곡선의 바디와 선명한 색 조합이 돋보이는 하모니입니다.

여러 색이 자연스럽게 이어지는 바디 패턴 덕분에 보는 방향과 빛에 따라 분위기가 달라지는데요. 책상 위에서도 존재감이 확실한 만년필을 찾거나, 한 자루만으로 포인트가 되는 디자인을 좋아하신다면 눈여겨보기 좋은 모델입니다.

[사진 4 | 하모니 전체 모습]
[사진 5 | 하모니 바디 패턴 확대]
[사진 6 | 하모니 캡·클립·펜촉 디테일]

오퍼스88 재즈 만년필

재즈는 단정한 실루엣과 클래식한 분위기가 매력적인 모델입니다.

라이트 블루는 산뜻하고 부드러운 인상, 레드는 또렷하고 선명한 인상, 블랙은 차분하고 묵직한 인상을 보여줍니다. 같은 형태라도 컬러에 따라 느낌이 확연히 달라서 평소 사용하는 노트나 잉크 색상에 맞춰 고르기 좋답니다.

화려한 패턴보다는 깔끔한 디자인을 선호하면서도 컬러로 개성을 표현하고 싶은 분께 추천드립니다.

[사진 7 | 재즈 라이트 블루·레드·블랙 전체 비교]
[사진 8 | 재즈 세 컬러를 가까이 촬영한 컷]
[사진 9 | 재즈 캡과 클립 디테일]
[사진 10 | 재즈를 손에 쥔 크기 비교]

오퍼스88 사파이어 데모 만년필

잉크가 담긴 모습을 감상하는 즐거움을 중요하게 생각한다면 사파이어 데모도 함께 살펴보세요.

투명감이 살아 있는 바디와 시원한 블루 컬러가 어우러져 여름철에 사용하기에도 잘 어울리는 인상을 보여줍니다. 어떤 잉크를 채우느냐에 따라 펜 전체의 분위기가 달라지는 점도 투명 만년필만의 재미인데요.

블루 계열 잉크를 채워 통일감 있게 사용하거나, 대비되는 색상의 잉크를 넣어 새로운 조합을 만들어보셔도 좋습니다.💙

[사진 11 | 사파이어 데모 전체 모습]
[사진 12 | 투명 바디와 내부가 보이는 확대 컷]
[사진 13 | 잉크를 채운 뒤의 모습]
[사진 14 | 빛을 통과시킨 바디 디테일]
[사진 15 | 블루 계열 잉크와 함께 연출한 컷]

어떤 오퍼스88가 잘 맞을까요?

패턴과 컬러가 강하게 드러나는 디자인을 좋아한다면 하모니, 단정한 실루엣과 컬러 선택을 중요하게 본다면 재즈, 잉크가 담긴 모습까지 즐기고 싶다면 사파이어 데모를 먼저 비교해 보세요.

사진으로 볼 때와 실제 손에 쥐었을 때는 바디의 크기와 무게감, 그립에서 느껴지는 인상이 달라질 수 있습니다. 평소 사용하는 펜과 크기를 비교하거나 직접 쥐어보면 선택이 한결 쉬워진답니다.

[사진 16 | 세 모델의 길이와 굵기 비교]
[사진 17 | 캡을 닫았을 때와 열었을 때 비교]
[사진 18 | 손으로 그립을 잡은 모습]

필기감도 직접 비교해 보세요

만년필은 같은 모델이라도 펜촉 굵기와 사용하는 잉크, 종이에 따라 선의 느낌이 달라집니다.

모델별로 선택 가능한 펜촉 옵션과 현재 재고가 다를 수 있으니 자사몰에서 옵션을 확인하고, 오프라인 매장에서는 실제 필기 굵기와 손에 닿는 느낌을 함께 비교해 보세요. 평소 자주 쓰는 노트를 가져오시면 익숙한 종이 위에서 살펴보실 수 있습니다.✍️

[사진 19 | 모델별 펜촉 확대 비교]
[사진 20 | 같은 잉크와 종이로 작성한 필기 샘플]
[사진 21 | 만년필과 어울리는 잉크를 함께 배치한 컷]

오랜만에 다시 만나는 오퍼스88 만년필!

하모니와 재즈, 사파이어 데모처럼 각기 다른 매력을 가진 모델들을 지금 블루블랙 펜샵에서 만나보세요. 직접 비교해 보며 오래 함께 기록할 한 자루를 골라보시길 바랍니다.😊

[사진 22 | 블루블랙 매장 진열 또는 최종 대표 컷]

※ 재입고 제품과 펜촉 옵션은 실시간 재고에 따라 달라질 수 있습니다. 정확한 판매 상태는 자사몰 또는 매장에서 확인해 주세요.`;

  const photos = [
    '재입고된 오퍼스88 만년필 전체 대표 컷', '오퍼스88 로고와 캡 디테일', '재입고 제품 일렬 배열',
    '하모니 전체 모습', '하모니 바디 패턴 확대', '하모니 캡·클립·펜촉 디테일',
    '재즈 라이트 블루·레드·블랙 비교', '재즈 세 컬러 확대', '재즈 캡과 클립 디테일', '재즈를 손에 쥔 크기',
    '사파이어 데모 전체', '투명 바디와 내부 확대', '잉크를 채운 모습', '빛을 통과시킨 바디', '블루 잉크 연출',
    '세 모델 길이와 굵기 비교', '캡을 닫고 연 모습', '손으로 그립을 잡은 모습',
    '모델별 펜촉 확대', '동일 잉크·종이 필기 샘플', '추천 잉크와 함께 배치', '매장 진열 또는 최종 대표 컷'
  ];

  async function loadAnalysis() {
    if (analysis) return analysis;
    try {
      const response = await fetch('./naver-blog-latest-analysis.json', { cache: 'no-store' });
      if (!response.ok) throw new Error();
      analysis = await response.json();
      return analysis;
    } catch { return null; }
  }

  function recentListMarkup(data) {
    return data.posts.map((post, index) => `
      <a href="${escapeHTML(post.link)}" target="_blank" rel="noopener noreferrer">
        <b>${String(index + 1).padStart(2, '0')}</b>
        <span><strong>${escapeHTML(post.title)}</strong><small>${escapeHTML(post.published.slice(5, 16))} · 본문 ${Number(post.text_length || 0).toLocaleString()}자 · 사진 ${post.image_count_effective ?? post.image_count_page ?? post.image_count_rss ?? 0}장</small></span>
        <em>↗</em>
      </a>`).join('');
  }

  async function enhance() {
    if (location.hash.replace(/^#\//, '') !== 'admin') return;
    const section = document.getElementById('july-blog-workbench');
    if (!section || section.dataset.enhanced === 'true') return;

    const textarea = section.querySelector('#july-opus-blog-draft');
    if (textarea) textarea.value = enhancedDraft;

    const draftBadge = section.querySelector('[data-july-blog-panel="draft"] .july-blog-card-head > b');
    if (draftBadge) draftBadge.textContent = `${enhancedDraft.length.toLocaleString()}자 · 사진 22장`;

    const photoCard = section.querySelector('.july-photo-card');
    const photoList = photoCard?.querySelector('ol');
    if (photoList) photoList.innerHTML = photos.map((item) => `<li>${escapeHTML(item)}</li>`).join('');
    const photoBadge = photoCard?.querySelector('.july-blog-card-head > b');
    if (photoBadge) photoBadge.textContent = '22컷';

    const data = await loadAnalysis();
    if (data?.summary) {
      const cards = section.querySelectorAll('.july-blog-analysis-summary > div');
      const summaryItems = [
        ['확인 글', `${data.count}개`],
        ['평균 본문', `${Number(data.summary.average_text_length || 0).toLocaleString()}자`],
        ['평균 사진', `${data.summary.average_image_count_effective || data.summary.average_image_count_page || 0}장`],
        ['말투', '직원 인사 + 친근한 소개']
      ];
      cards.forEach((card, index) => {
        const item = summaryItems[index];
        if (item) card.innerHTML = `<span>${escapeHTML(item[0])}</span><strong>${escapeHTML(item[1])}</strong>`;
      });

      const recent = section.querySelector('.july-blog-recent-list');
      if (recent) recent.innerHTML = recentListMarkup(data);

      const note = section.querySelector('.july-blog-analysis-note');
      if (note) note.innerHTML = `
        <strong>최신 6개 글에서 확인한 작성 패턴</strong>
        <p>평균 본문은 약 ${Number(data.summary.average_text_length || 0).toLocaleString()}자, 콘텐츠 사진은 평균 약 ${data.summary.average_image_count_effective || data.summary.average_image_count_page || 0}장입니다. 직원 이름으로 인사한 뒤 소식을 바로 알리고, 제품별 소제목과 짧은 문단을 반복하며 마지막에 매장 또는 구매를 안내합니다.</p>
        <small>이번 오퍼스88 초안은 전체 ${enhancedDraft.length.toLocaleString()}자와 사진 22장으로 구성해 최근 글 평균에 맞췄습니다. 네이버 모바일 본문 DOM에서 장식·프로필 이미지를 제외하고 집계했습니다.</small>`;
    }

    section.dataset.enhanced = 'true';
  }

  function schedule() {
    clearTimeout(timer);
    timer = setTimeout(enhance, 100);
  }

  window.addEventListener('DOMContentLoaded', schedule, { once: true });
  window.addEventListener('hashchange', schedule);
  new MutationObserver(schedule).observe(document.documentElement, { childList: true, subtree: true });
})();
